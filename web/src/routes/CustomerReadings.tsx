import Placeholder from "../components/Placeholder";

export default function CustomerReadings() {
  return <Placeholder title="Readings" note="Interval chart from /api/sites/{id}/readings — Recharts area chart, import vs export vs generation." />;
}
