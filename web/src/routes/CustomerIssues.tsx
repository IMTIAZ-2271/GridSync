import Placeholder from "../components/Placeholder";

export default function CustomerIssues() {
  return <Placeholder title="Report an issue" note="Form posting to /api/issues, plus this site's existing issues." />;
}
