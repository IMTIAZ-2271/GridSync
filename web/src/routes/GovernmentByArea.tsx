import Placeholder from "../components/Placeholder";

export default function GovernmentByArea() {
  return <Placeholder title="By area" note="District rollup from /api/analytics/by-area — site counts and import/export totals." />;
}
